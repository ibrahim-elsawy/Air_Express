import request from 'supertest';
// import app from '../../src/app';

describe("/user/:id", () => {
	describe("GET account ", () => { 
		it("should return account by id", () => {
			// postponed as DI design can make unit testing cover all paths
		});
	});
});